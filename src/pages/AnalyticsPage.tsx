import { EmptyState } from '../components/FeedbackState';

export function AnalyticsPage() {
  return (
    <EmptyState
      title="Analytics"
      description="Analytics widgets are intentionally left as a foundation placeholder."
    />
  );
}
